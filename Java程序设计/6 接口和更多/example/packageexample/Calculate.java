package mypackage;//包的名称为mypackage
public class Calculate {
		int a;
		public Calculate()
		{a=10;}
		public double volume(double height,double 	width,double depth)
		{ return height*width*depth; }
		int add(int x,int y)
		{ return x+y; }	
 	    protected float mul(float x,float y)
		{ return x*y; }
}
